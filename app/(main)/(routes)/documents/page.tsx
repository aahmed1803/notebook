"use client"

const DocumentsPage = ({ text = "Default Filler Text" }) => {
  return (
    <div>
      <h1>{text}</h1>
      <p>This is a simple filler component for testing purposes.</p>
    </div>
  )
};

export default DocumentsPage;

